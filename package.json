{
  "name": "teacher",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "assets:clean": "node scripts/cleanup-assets.mjs",
    "assets:clean:city": "node scripts/cleanup-assets.mjs --mode scenery",
    "assets:clean:teacher": "node scripts/cleanup-assets.mjs --mode character",
    "predev": "npm run assets:clean",
    "dev": "vite",
    "prebuild": "npm run assets:clean",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.2.2"
  },
  "dependencies": {
    "gsap": "^3.15.0",
    "pixi.js": "^8.20.1"
  }
}
